@if (SelectedItem != null && SelectedItem._id > 0)
{
    @if (SelectedItem.parkAttractions.Count > 0)
    {
        <span>@SelectedItem.name played in the following <b>park attractions</b> @(WinCategory ? "👑" : "")</span>
        <table>
            @foreach (var item in SelectedItem.parkAttractions)
            {
                <tr>
                    <td>@(SelectedItem.parkAttractions.IndexOf(item) + 1)) @item</td>
                </tr>
            }
        </table>
    }
    else
    {
        <span>Oh no! It seems this character does not have any park attractions 🥺</span>
    }
}

@code {
    [Parameter]
    public Characters SelectedItem { get; set; }
    [Parameter]
    public bool WinCategory { get; set; } = false;
}
